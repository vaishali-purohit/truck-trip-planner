# Django
DJANGO_SECRET_KEY=replace_me_with_a_real_secret
DJANGO_DEBUG=false
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# CORS / frontend
FRONTEND_ORIGIN=http://localhost:5173

# Location search (autocomplete)
GEOCODER_NOMINATIM_BASE_URL=https://nominatim.openstreetmap.org
GEOCODER_USER_AGENT=truck-trip-planner/1.0 (contact: dev@local)
LOCATION_SEARCH_TIMEOUT_SECONDS=8

# Routing (OSRM)
ROUTER_OSRM_BASE_URL=https://router.project-osrm.org
ROUTE_TIMEOUT_SECONDS=45

# Abuse protection (optional)
# API_KEY=replace_with_strong_random_value
# DJANGO_THROTTLE_ANON=60/min
